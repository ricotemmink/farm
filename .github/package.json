{
  "name": "synthorg-ci-tools",
  "version": "0.0.0",
  "private": true,
  "description": "CI-only tooling (hash-pinned via lockfile for OSSF Scorecard compliance)",
  "dependencies": {
    "wrangler": "4.78.0"
  }
}
