VITE_API_BASE_URL=

# Dev-only: bypass auth and setup guards (set to "true" to enable)
# Injects a fake token and user so you can browse the dashboard without a backend.
# Only works when running `npm run dev` (import.meta.env.DEV must be true).
# VITE_DEV_AUTH_BYPASS=true
