# Generated by SynthOrg CLI dev
# Do not edit manually — run 'synthorg init' to regenerate.

services:
  backend:
    image: ghcr.io/aureliolo/synthorg-backend@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
    ports:
      - "8000:8000"
    volumes:
      - synthorg-data:/data
    environment:
      SYNTHORG_HOST: "0.0.0.0"
      SYNTHORG_PORT: "8000"
      SYNTHORG_DB_PATH: "/data/synthorg.db"
      SYNTHORG_MEMORY_DIR: "/data/memory"
      SYNTHORG_PERSISTENCE_BACKEND: "sqlite"
      SYNTHORG_MEMORY_BACKEND: "mem0"
      SYNTHORG_LOG_DIR: "/data/logs"
      MEM0_TELEMETRY: "false"
      SYNTHORG_LOG_LEVEL: "info"
    user: "65532:65532"
    # CIS Docker Benchmark v1.6.0 hardening (5.3, 5.12, 5.25, 5.28)
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,nodev,size=64m
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
          pids: 256
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    # Healthcheck assumes internal port is always 8000 (container-side port
    # mapping is fixed). Dockerfile healthcheck reads UVICORN_PORT dynamically,
    # but compose-level exec form cannot expand env vars without a shell.
    healthcheck:
      test: ["CMD", "/usr/bin/python", "-c", "import json,urllib.request,sys; r=urllib.request.urlopen('http://localhost:8000/api/v1/health'); d=json.loads(r.read()); sys.exit(0 if d.get('data',{}).get('status')=='ok' else 1)"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  web:
    image: ghcr.io/aureliolo/synthorg-web@sha256:bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
    ports:
      - "3000:8080"
    depends_on:
      backend:
        condition: service_healthy
    user: "101:101"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,nodev,size=16m
      - /var/cache/nginx:noexec,nosuid,nodev,size=32m
      - /var/run:noexec,nosuid,nodev,size=1m
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
          pids: 64
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  default:
    name: synthorg-net

volumes:
  synthorg-data:
