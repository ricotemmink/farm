host: 0.0.0.0
port: 4222
http_port: 8222
jetstream {
  store_dir: /data
}
# 16MB covers LLM agent outputs, meeting transcripts, and tool results
# that routinely exceed the 1MB NATS default; well under the 64MB ceiling.
max_payload: 16MB
